type CoinCard = {
  name: string;
  ticker: string;
  icon?: string;
  description: string;
  URL: string;
};

export const getCoinCards = (): CoinCard[] => {
  // TODO: fetch, sort and return as name/description objects.
  return [];
};
